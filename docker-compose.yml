version: '3.8'

services:
  y-websocket-server:
    build: ./apps/server
    ports:
      - "1234:1234"
    environment:
      - HOST=0.0.0.0
      - PORT=1234
    restart: unless-stopped
    container_name: seasonal-board-game-server